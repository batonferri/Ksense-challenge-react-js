export type Columns = {
  title: string;
  dataIndex: string;
  key: string;
};

export type PostCardType = {
  title: string;
  body: string;
};

export type UserType = {
  id: number;
  username: string;
};
